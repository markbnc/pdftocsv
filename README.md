# pdftocsv

Helper application to convert PDF files received from the insurance company to CSV file.  The main reason for this is so the data can be imported into Excel.

## Syntax
```
pdftocsv -c <config-file> <pdf-file>
```
